<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:MauiApp3.ViewModels"
             xmlns:models="clr-namespace:MauiApp3.Models"
             xmlns:converters="clr-namespace:MauiApp3.Converters"
             xmlns:controls="clr-namespace:MauiApp3.Controls"
             xmlns:behaviors="clr-namespace:MauiApp3.Behaviors"
             x:Class="MauiApp3.MainPage"
             x:DataType="viewmodels:MainViewModel">

    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:FileSizeConverter x:Key="FileSizeConverter"/>
            <converters:BoolToWidthConverter x:Key="BoolToWidthConverter"/>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid>
        <!-- Main Grid with 3 columns: Drawer, Model Grid (larger), Renderer (smaller) -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="{Binding DrawerWidth}"/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <!-- LEFT DRAWER -->
        <Grid Grid.Column="0" 
              BackgroundColor="#2C2C2C"
              IsVisible="{Binding IsDrawerOpen}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Drawer Header -->
            <VerticalStackLayout Grid.Row="0" 
                               Padding="20"
                               BackgroundColor="#1E1E1E">
                <Label Text="3D Model DB"
                       FontSize="20"
                       FontAttributes="Bold"
                       TextColor="White"
                       Margin="0,0,0,10"/>
                
                <BoxView HeightRequest="1" 
                         BackgroundColor="#444444"/>
            </VerticalStackLayout>

            <!-- Drawer Content -->
            <ScrollView Grid.Row="1" Padding="15">
                <VerticalStackLayout Spacing="15">
                    <Label Text="Library Statistics"
                           FontSize="16"
                           FontAttributes="Bold"
                           TextColor="White"
                           Margin="0,0,0,10"/>

                    <Frame BackgroundColor="#3C3C3C" 
                           Padding="15"
                           CornerRadius="8"
                           HasShadow="False">
                        <VerticalStackLayout Spacing="8">
                            <Label TextColor="#CCCCCC">
                                <Label.FormattedText>
                                    <FormattedString>
                                        <Span Text="Total Models: " FontAttributes="Bold"/>
                                        <Span Text="{Binding Models.Count}"/>
                                    </FormattedString>
                                </Label.FormattedText>
                            </Label>
                            
                            <Label TextColor="#CCCCCC">
                                <Label.FormattedText>
                                    <FormattedString>
                                        <Span Text="Projects: " FontAttributes="Bold"/>
                                        <Span Text="{Binding Projects.Count}"/>
                                    </FormattedString>
                                </Label.FormattedText>
                            </Label>

                            <Label Text="Quick Actions"
                                   FontSize="14"
                                   FontAttributes="Bold"
                                   TextColor="White"
                                   Margin="0,10,0,5"/>
                            

                            <Button Text="📁 Upload Model"
                                    Clicked="OnUploadButtonClicked"
                                    BackgroundColor="#0078D4"
                                    TextColor="White"
                                    CornerRadius="6"
                                    Padding="10"/>
                            
                            <Button Text="📂 New Project"
                                    Command="{Binding CreateProjectCommand}"
                                    BackgroundColor="#107C10"
                                    TextColor="White"
                                    CornerRadius="6"
                                    Padding="10"/>
                        </VerticalStackLayout>
                    </Frame>

                    <!-- Projects List -->
                    <Label Text="Projects"
                           FontSize="14"
                           FontAttributes="Bold"
                           TextColor="White"
                           Margin="0,10,0,5"/>

                    <Frame BackgroundColor="#3C3C3C" 
                           Padding="10"
                           CornerRadius="8"
                           HasShadow="False">
                        <VerticalStackLayout Spacing="5">
                            <!-- All Models Button -->
                            <Frame BackgroundColor="#0078D4"
                                   Padding="8"
                                   CornerRadius="6"
                                   HasShadow="False"
                                   Margin="0,0,0,5">
                                <Frame.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding SelectProjectCommand}"
                                                        CommandParameter="{x:Null}"/>
                                </Frame.GestureRecognizers>
                                <Label Text="📚 All Models"
                                       TextColor="White"
                                       FontAttributes="Bold"/>
                            </Frame>
                            
                            <!-- Projects Collection -->
                            <CollectionView ItemsSource="{Binding Projects}">
                                <CollectionView.ItemTemplate>
                                    <DataTemplate x:DataType="models:Project">
                                        <SwipeView>
                                            <SwipeView.RightItems>
                                                <SwipeItems>
                                                    <SwipeItem Text="Delete"
                                                             BackgroundColor="#C42B1C"
                                                             Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:MainViewModel}}, Path=DeleteProjectCommand}"
                                                             CommandParameter="{Binding .}"/>
                                                </SwipeItems>
                                            </SwipeView.RightItems>
                                            
                                            <Frame BackgroundColor="#2C2C2C"
                                                   Padding="8"
                                                   CornerRadius="6"
                                                   HasShadow="False"
                                                   Margin="0,2">
                                                <Frame.GestureRecognizers>
                                                    <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:MainViewModel}}, Path=SelectProjectCommand}"
                                                                        CommandParameter="{Binding .}"/>
                                                </Frame.GestureRecognizers>
                                                <Grid ColumnDefinitions="Auto,*,Auto">
                                                    <BoxView Grid.Column="0"
                                                           Color="{Binding Color}"
                                                           WidthRequest="4"
                                                           HeightRequest="30"
                                                           CornerRadius="2"
                                                           Margin="0,0,10,0"/>
                                                    <Label Grid.Column="1"
                                                           Text="{Binding Name}"
                                                           TextColor="White"
                                                           VerticalOptions="Center"/>
                                                    <Label Grid.Column="2"
                                                           Text="{Binding ModelIds.Count, StringFormat='{0}'}"
                                                           TextColor="#AAAAAA"
                                                           FontSize="12"
                                                           VerticalOptions="Center"/>
                                                </Grid>
                                            </Frame>
                                        </SwipeView>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                                <CollectionView.EmptyView>
                                    <Label Text="No projects yet"
                                           TextColor="#888888"
                                           FontSize="12"
                                           FontAttributes="Italic"
                                           Padding="10"/>
                                </CollectionView.EmptyView>
                            </CollectionView>
                        </VerticalStackLayout>
                    </Frame>

                    <Label Text="File Filters"
                           FontSize="14"
                           FontAttributes="Bold"
                           TextColor="White"
                           Margin="0,10,0,5"/>

                    <Frame BackgroundColor="#3C3C3C" 
                           Padding="10"
                           CornerRadius="8"
                           HasShadow="False">
                        <VerticalStackLayout Spacing="5">
                            <!-- All Files Filter -->
                            <Frame Padding="8"
                                   CornerRadius="6"
                                   HasShadow="False"
                                   Margin="0,2">
                                <Frame.Triggers>
                                    <DataTrigger TargetType="Frame"
                                               Binding="{Binding SelectedFileFilter}"
                                               Value="{x:Null}">
                                        <Setter Property="BackgroundColor" Value="#0078D4"/>
                                    </DataTrigger>
                                </Frame.Triggers>
                                <Frame.Behaviors>
                                    <behaviors:EventToCommandBehavior EventName="Tapped"
                                                                    Command="{Binding SelectFileFilterCommand}"
                                                                    CommandParameter="{x:Null}"/>
                                </Frame.Behaviors>
                                <Frame.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding SelectFileFilterCommand}"
                                                        CommandParameter="{x:Null}"/>
                                </Frame.GestureRecognizers>
                                <Label Text="⬜ All Files"
                                       TextColor="White"
                                       Padding="5"/>
                            </Frame>
                            
                            <!-- STL Files Filter -->
                            <Frame Padding="8"
                                   CornerRadius="6"
                                   HasShadow="False"
                                   BackgroundColor="#2C2C2C"
                                   Margin="0,2">
                                <Frame.Triggers>
                                    <DataTrigger TargetType="Frame"
                                               Binding="{Binding SelectedFileFilter}"
                                               Value="STL">
                                        <Setter Property="BackgroundColor" Value="#0078D4"/>
                                    </DataTrigger>
                                </Frame.Triggers>
                                <Frame.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding SelectFileFilterCommand}"
                                                        CommandParameter="STL"/>
                                </Frame.GestureRecognizers>
                                <Label Text="🔹 STL Files"
                                       TextColor="White"
                                       Padding="5"/>
                            </Frame>
                            
                            <!-- 3MF Files Filter -->
                            <Frame Padding="8"
                                   CornerRadius="6"
                                   HasShadow="False"
                                   BackgroundColor="#2C2C2C"
                                   Margin="0,2">
                                <Frame.Triggers>
                                    <DataTrigger TargetType="Frame"
                                               Binding="{Binding SelectedFileFilter}"
                                               Value="3MF">
                                        <Setter Property="BackgroundColor" Value="#0078D4"/>
                                    </DataTrigger>
                                </Frame.Triggers>
                                <Frame.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding SelectFileFilterCommand}"
                                                        CommandParameter="3MF"/>
                                </Frame.GestureRecognizers>
                                <Label Text="🔸 3MF Files"
                                       TextColor="White"
                                       Padding="5"/>
                            </Frame>
                        </VerticalStackLayout>
                    </Frame>
                </VerticalStackLayout>
            </ScrollView>

            <!-- Drawer Footer -->
            <VerticalStackLayout Grid.Row="2" 
                               Padding="15"
                               BackgroundColor="#1E1E1E"
                               Spacing="10">
                <BoxView HeightRequest="1" 
                         BackgroundColor="#444444"
                         Margin="0,0,0,10"/>
                
                <!-- Reset Database Button -->
                <Button Text="🗑️ Reset Database"
                        Command="{Binding ResetDatabaseCommand}"
                        BackgroundColor="#C42B1C"
                        TextColor="White"
                        CornerRadius="6"
                        Padding="10"
                        FontSize="12"
                        Margin="0,0,0,10"/>
                
                <Label Text="© 2025 Bob Koertge"
                       FontSize="12"
                       TextColor="#888888"
                       HorizontalOptions="Center"/>
            </VerticalStackLayout>
        </Grid>

        <!-- CENTER - MODEL GRID (Now larger) -->
        <Grid Grid.Column="1" 
              BackgroundColor="#1E1E1E"
              Padding="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header with Toggle Button -->
            <Grid Grid.Row="0" 
                  Padding="10"
                  BackgroundColor="#2C2C2C"
                  Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Text="☰"
                        Command="{Binding ToggleDrawerCommand}"
                        BackgroundColor="#3C3C3C"
                        TextColor="White"
                        WidthRequest="40"
                        HeightRequest="40"
                        CornerRadius="6"
                        Padding="0"
                        Margin="0,0,10,0"/>

                <Label Grid.Column="1"
                       Text="3D Models Library"
                       FontSize="18"
                       FontAttributes="Bold"
                       TextColor="White"
                       VerticalOptions="Center"/>
            </Grid>

            <!-- Models Grid -->
            <ScrollView Grid.Row="1">
                <CollectionView ItemsSource="{Binding Models}"
                               SelectionMode="Single"
                               SelectionChangedCommand="{Binding SelectModelCommand}"
                               SelectionChangedCommandParameter="{Binding SelectedItem, Source={RelativeSource Self}}">
                    <CollectionView.ItemsLayout>
                        <GridItemsLayout Orientation="Vertical"
                                       Span="3"
                                       HorizontalItemSpacing="10"
                                       VerticalItemSpacing="10"/>
                    </CollectionView.ItemsLayout>
                    
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="models:Model3DFile">
                            <Frame BackgroundColor="#2C2C2C"
                                   Padding="15"
                                   CornerRadius="10"
                                   HasShadow="True"
                                   BorderColor="#0078D4">
                                <Frame.Triggers>
                                    <DataTrigger TargetType="Frame"
                                               Binding="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:MainViewModel}}, Path=SelectedModel.Id}"
                                               Value="{Binding Id}">
                                        <Setter Property="BorderColor" Value="#0078D4"/>
                                        <Setter Property="BackgroundColor" Value="#3C3C3C"/>
                                    </DataTrigger>
                                </Frame.Triggers>
                                <Frame.GestureRecognizers>
                                    <TapGestureRecognizer NumberOfTapsRequired="2"
                                                        Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:MainViewModel}}, Path=OpenDetailCommand}"
                                                        CommandParameter="{Binding .}"/>
                                </Frame.GestureRecognizers>
                                
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="100"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- Thumbnail with generated image or placeholder -->
                                    <Frame Grid.Row="0"
                                           BackgroundColor="#1E1E1E"
                                           Padding="0"
                                           CornerRadius="6"
                                           HasShadow="False">
                                        <Grid>
                                            <!-- Thumbnail Image -->
                                            <Image Source="{Binding ThumbnailData, Converter={StaticResource ByteArrayToImageSourceConverter}}"
                                                   Aspect="AspectFit"
                                                   HorizontalOptions="Fill"
                                                   VerticalOptions="Fill"
                                                   IsVisible="{Binding ThumbnailData, Converter={StaticResource IsNotNullConverter}}"/>
                                            
                                            <!-- Fallback placeholder if no thumbnail -->
                                            <Label Text="🎲"
                                                   FontSize="50"
                                                   HorizontalOptions="Center"
                                                   VerticalOptions="Center"
                                                   TextColor="#666666"
                                                   IsVisible="{Binding ThumbnailData, Converter={StaticResource IsNullConverter}}"/>
                                        </Grid>
                                    </Frame>

                                    <!-- File Name -->
                                    <Label Grid.Row="1"
                                           Text="{Binding Name}"
                                           FontSize="14"
                                           FontAttributes="Bold"
                                           TextColor="White"
                                           LineBreakMode="TailTruncation"
                                           Margin="0,10,0,5"/>

                                    <!-- Project Badge (if assigned) -->
                                    <Frame Grid.Row="2"
                                           BackgroundColor="#107C10"
                                           Padding="5,2"
                                           CornerRadius="4"
                                           HasShadow="False"
                                           Margin="0,0,0,5"
                                           IsVisible="{Binding ProjectId, Converter={StaticResource IsNotNullConverter}}">
                                        <HorizontalStackLayout Spacing="5">
                                            <Label Text="📂"
                                                   FontSize="10"
                                                   TextColor="White"
                                                   VerticalOptions="Center"/>
                                            <Label Text="{Binding ProjectName}"
                                                   FontSize="10"
                                                   FontAttributes="Bold"
                                                   TextColor="White"
                                                   VerticalOptions="Center"
                                                   LineBreakMode="TailTruncation"
                                                   MaximumWidthRequest="120"/>
                                        </HorizontalStackLayout>
                                    </Frame>

                                    <!-- File Type Badge -->
                                    <HorizontalStackLayout Grid.Row="3" Spacing="5" Margin="0,0,0,5">
                                        <Frame BackgroundColor="#0078D4"
                                               Padding="5,2"
                                               CornerRadius="4"
                                               HasShadow="False">
                                            <Label Text="{Binding FileType}"
                                                   FontSize="10"
                                                   FontAttributes="Bold"
                                                   TextColor="White"/>
                                        </Frame>
                                        <Label Text="{Binding FileSize, Converter={StaticResource FileSizeConverter}}"
                                               FontSize="12"
                                               TextColor="#AAAAAA"
                                               VerticalOptions="Center"/>
                                    </HorizontalStackLayout>

                                    <!-- Tags Display -->
                                    <FlexLayout Grid.Row="4"
                                               Wrap="Wrap"
                                               Direction="Row"
                                               JustifyContent="Start"
                                               Margin="0,5,0,5">
                                        <BindableLayout.ItemsSource>
                                            <Binding Path="Tags"/>
                                        </BindableLayout.ItemsSource>
                                        <BindableLayout.ItemTemplate>
                                            <DataTemplate x:DataType="x:String">
                                                <Frame BackgroundColor="#0078D4"
                                                       Padding="10,5"
                                                       CornerRadius="12"
                                                       HasShadow="False"
                                                       Margin="3,2">
                                                    <Label Text="{Binding .}"
                                                           FontSize="12"
                                                           FontAttributes="Bold"
                                                           TextColor="White"/>
                                                </Frame>
                                            </DataTemplate>
                                        </BindableLayout.ItemTemplate>
                                    </FlexLayout>

                                    <!-- Upload Date -->
                                    <Label Grid.Row="5"
                                           Text="{Binding UploadedDate, StringFormat='Uploaded: {0:MM/dd/yyyy}'}"
                                           FontSize="11"
                                           TextColor="#888888"/>
                                </Grid>
                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                    
                    <CollectionView.EmptyView>
                        <VerticalStackLayout HorizontalOptions="Center"
                                           VerticalOptions="Center"
                                           Padding="20">
                            <Label Text="📂"
                                   FontSize="60"
                                   HorizontalOptions="Center"
                                   TextColor="#666666"/>
                            <Label Text="No 3D models uploaded yet"
                                   FontSize="16"
                                   TextColor="#AAAAAA"
                                   HorizontalOptions="Center"
                                   Margin="0,10,0,0"/>
                            <Button Text="Upload Your First Model"
                                    Clicked="OnUploadButtonClicked"
                                    BackgroundColor="#0078D4"
                                    TextColor="White"
                                    Margin="0,20,0,0"
                                    CornerRadius="6"
                                    Padding="15,10"/>
                        </VerticalStackLayout>
                    </CollectionView.EmptyView>
                </CollectionView>
            </ScrollView>
        </Grid>

        <!-- RIGHT - 3D RENDERER (Now smaller) -->
        <Grid Grid.Column="2" 
              BackgroundColor="#252525"
              Padding="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Renderer Header -->
            <Frame Grid.Row="0"
                   BackgroundColor="#2C2C2C"
                   Padding="15"
                   CornerRadius="8"
                   HasShadow="False"
                   Margin="0,0,0,15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <VerticalStackLayout Grid.Column="0" Spacing="8">
                        <Label Text="Model Preview"
                               FontSize="18"
                               FontAttributes="Bold"
                               TextColor="White"/>
                        <Label Text="{Binding SelectedModel.Name, StringFormat='Viewing: {0}'}"
                               FontSize="12"
                               TextColor="#AAAAAA"
                               IsVisible="{Binding SelectedModel, Converter={StaticResource IsNotNullConverter}}"/>
                        
                        <!-- Project Badge (if model has project) -->
                        <Frame BackgroundColor="#107C10"
                               Padding="8,4"
                               CornerRadius="6"
                               HasShadow="False"
                               HorizontalOptions="Start"
                               IsVisible="{Binding SelectedModel.ProjectId, Converter={StaticResource IsNotNullConverter}}">
                            <HorizontalStackLayout Spacing="6">
                                <Label Text="📂"
                                       FontSize="12"
                                       TextColor="White"
                                       VerticalOptions="Center"/>
                                <Label Text="{Binding SelectedModel.ProjectName}"
                                       FontSize="12"
                                       FontAttributes="Bold"
                                       TextColor="White"
                                       VerticalOptions="Center"/>
                            </HorizontalStackLayout>
                        </Frame>
                        
                        <Label Text="Double-click a model to see full details"
                               FontSize="11"
                               TextColor="#666666"
                               FontAttributes="Italic"
                               Margin="0,5,0,0"/>
                    </VerticalStackLayout>
                </Grid>
            </Frame>

            <!-- 3D Viewer Area -->
            <Frame Grid.Row="1"
                   BackgroundColor="#1A1A1A"
                   Padding="0"
                   CornerRadius="10"
                   HasShadow="False"
                   BorderColor="#444444">
                <Grid>
                    <!-- Empty state -->
                    <VerticalStackLayout HorizontalOptions="Center"
                                       VerticalOptions="Center"
                                       IsVisible="{Binding SelectedModel, Converter={StaticResource IsNullConverter}}">
                        <Label Text="🎲"
                               FontSize="80"
                               TextColor="#444444"
                               HorizontalOptions="Center"/>
                        <Label Text="Select a model to view"
                               FontSize="16"
                               TextColor="#666666"
                               HorizontalOptions="Center"
                               Margin="0,20,0,0"/>
                    </VerticalStackLayout>

                    <!-- Model Preview Image (Static Snapshot) -->
                    <Image Source="{Binding SelectedModel.ThumbnailData, Converter={StaticResource ByteArrayToImageSourceConverter}}"
                           Aspect="AspectFit"
                           HorizontalOptions="Fill"
                           VerticalOptions="Fill"
                           BackgroundColor="#1A1A1A"
                           IsVisible="{Binding SelectedModel, Converter={StaticResource IsNotNullConverter}}"/>

                    <!-- Loading Indicator -->
                    <ActivityIndicator IsRunning="{Binding IsLoading}"
                                     IsVisible="{Binding IsLoading}"
                                     Color="#0078D4"
                                     WidthRequest="50"
                                     HeightRequest="50"
                                     HorizontalOptions="Center"
                                     VerticalOptions="Center"/>
                </Grid>
            </Frame>

            <!-- Model Info Panel -->
            <ScrollView Grid.Row="2"
                       Margin="0,15,0,0"
                       MaximumHeightRequest="300"
                       IsVisible="{Binding SelectedModel, Converter={StaticResource IsNotNullConverter}}">
                <Frame BackgroundColor="#2C2C2C"
                       Padding="15"
                       CornerRadius="8"
                       HasShadow="False">
                    <VerticalStackLayout Spacing="15">
                        <!-- File Information and Actions Row -->
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <VerticalStackLayout Grid.Row="0" Grid.Column="0" Spacing="5">
                                <Label Text="File Information"
                                       FontSize="14"
                                       FontAttributes="Bold"
                                       TextColor="White"/>
                                <Label TextColor="#CCCCCC" FontSize="12">
                                    <Label.FormattedText>
                                        <FormattedString>
                                            <Span Text="Type: "/>
                                            <Span Text="{Binding SelectedModel.FileType}" FontAttributes="Bold"/>
                                        </FormattedString>
                                    </Label.FormattedText>
                                </Label>
                                <Label TextColor="#CCCCCC" FontSize="12">
                                    <Label.FormattedText>
                                        <FormattedString>
                                            <Span Text="Size: "/>
                                            <Span Text="{Binding SelectedModel.FileSize, Converter={StaticResource FileSizeConverter}}" FontAttributes="Bold"/>
                                        </FormattedString>
                                    </Label.FormattedText>
                                </Label>
                                <Label TextColor="#CCCCCC" FontSize="12"
                                       IsVisible="{Binding SelectedModel.ParsedModel, Converter={StaticResource IsNotNullConverter}}">
                                    <Label.FormattedText>
                                        <FormattedString>
                                            <Span Text="Triangles: "/>
                                            <Span Text="{Binding SelectedModel.ParsedModel.Triangles.Count}" FontAttributes="Bold"/>
                                        </FormattedString>
                                    </Label.FormattedText>
                                </Label>
                            </VerticalStackLayout>

                            <VerticalStackLayout Grid.Row="0" Grid.Column="1" Spacing="5">
                                <Label Text="Actions"
                                       FontSize="14"
                                       FontAttributes="Bold"
                                       TextColor="White"/>
                                <HorizontalStackLayout Spacing="5">
                                    <Button Text="📂 Project"
                                            FontSize="11"
                                            Command="{Binding AddModelToProjectCommand}"
                                            CommandParameter="{Binding SelectedModel}"
                                            BackgroundColor="#107C10"
                                            TextColor="White"
                                            CornerRadius="4"
                                            Padding="10,5"
                                            IsVisible="{Binding SelectedModel.ProjectId, Converter={StaticResource IsNullConverter}}"/>
                                    <Button Text="Remove from Project"
                                            FontSize="11"
                                            Command="{Binding RemoveModelFromProjectCommand}"
                                            CommandParameter="{Binding SelectedModel}"
                                            BackgroundColor="#CA5010"
                                            TextColor="White"
                                            CornerRadius="4"
                                            Padding="10,5"
                                            IsVisible="{Binding SelectedModel.ProjectId, Converter={StaticResource IsNotNullConverter}}"/>
                                    <Button Text="Export"
                                            FontSize="11"
                                            BackgroundColor="#0078D4"
                                            TextColor="White"
                                            CornerRadius="4"
                                            Padding="10,5"/>
                                    <Button Text="Delete"
                                            FontSize="11"
                                            Command="{Binding DeleteModelCommand}"
                                            CommandParameter="{Binding SelectedModel}"
                                            BackgroundColor="#C42B1C"
                                            TextColor="White"
                                            CornerRadius="4"
                                            Padding="10,5"/>
                                </HorizontalStackLayout>
                            </VerticalStackLayout>
                        </Grid>

                        <BoxView HeightRequest="1" 
                                 BackgroundColor="#444444"/>

                        <!-- Tags Management Section -->
                        <VerticalStackLayout Spacing="10">
                            <Label Text="Tags"
                                   FontSize="14"
                                   FontAttributes="Bold"
                                   TextColor="White"/>

                            <!-- Tag Input -->
                            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="5">
                                <Entry Grid.Column="0"
                                       Text="{Binding NewTagText}"
                                       Placeholder="Add new tag..."
                                       PlaceholderColor="#888888"
                                       TextColor="White"
                                       BackgroundColor="#3C3C3C"
                                       FontSize="12"
                                       HeightRequest="34"
                                       ReturnCommand="{Binding AddTagCommand}"/>
                                <Button Grid.Column="1"
                                        Text="+"
                                        Command="{Binding AddTagCommand}"
                                        BackgroundColor="#0078D4"
                                        TextColor="White"
                                        WidthRequest="34"
                                        HeightRequest="34"
                                        CornerRadius="4"
                                        Padding="0"
                                        FontSize="18"
                                        FontAttributes="Bold"/>
                            </Grid>

                            <!-- Tags Display -->
                            <FlexLayout Wrap="Wrap"
                                       Direction="Row"
                                       JustifyContent="Start"
                                       Margin="0,5,0,0">
                                <BindableLayout.ItemsSource>
                                    <Binding Path="SelectedModel.Tags"/>
                                </BindableLayout.ItemsSource>
                                <BindableLayout.ItemTemplate>
                                    <DataTemplate x:DataType="x:String">
                                        <Frame BackgroundColor="#0078D4"
                                               Padding="8,4"
                                               CornerRadius="12"
                                               HasShadow="False"
                                               Margin="3,2">
                                            <HorizontalStackLayout Spacing="6">
                                                <Label Text="{Binding .}"
                                                       FontSize="11"
                                                       FontAttributes="Bold"
                                                       TextColor="White"
                                                       VerticalOptions="Center"/>
                                                <Label Text="×"
                                                       FontSize="16"
                                                       FontAttributes="Bold"
                                                       TextColor="White"
                                                       VerticalOptions="Center">
                                                    <Label.GestureRecognizers>
                                                        <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:MainViewModel}}, Path=RemoveTagCommand}"
                                                                            CommandParameter="{Binding .}"/>
                                                    </Label.GestureRecognizers>
                                                </Label>
                                            </HorizontalStackLayout>
                                        </Frame>
                                    </DataTemplate>
                                </BindableLayout.ItemTemplate>
                                <BindableLayout.EmptyViewTemplate>
                                    <DataTemplate>
                                        <Label Text="No tags yet. Add some!"
                                               FontSize="11"
                                               TextColor="#888888"
                                               FontAttributes="Italic"
                                               Margin="5"/>
                                    </DataTemplate>
                                </BindableLayout.EmptyViewTemplate>
                            </FlexLayout>
                        </VerticalStackLayout>
                    </VerticalStackLayout>
                </Frame>
            </ScrollView>
        </Grid>

        <!-- Vertical Separators -->
        <BoxView Grid.Column="0"
                 WidthRequest="1"
                 BackgroundColor="#444444"
                 HorizontalOptions="End"
                 IsVisible="{Binding IsDrawerOpen}"/>
        
        <BoxView Grid.Column="1"
                 WidthRequest="1"
                 BackgroundColor="#444444"
                 HorizontalOptions="End"/>

        <!-- LOADING OVERLAY -->
        <Grid IsVisible="{Binding IsLoading}"
              BackgroundColor="#80000000"
              VerticalOptions="FillAndExpand"
              HorizontalOptions="FillAndExpand"
              ZIndex="100">
            <ActivityIndicator IsRunning="True"
                               Color="#0078D4"
                               WidthRequest="64"
                               HeightRequest="64"
                               VerticalOptions="Center"
                               HorizontalOptions="Center"/>
            <Label Text="Loading..."
                   TextColor="White"
                   FontSize="18"
                   VerticalOptions="Center"
                   HorizontalOptions="Center"
                   Margin="0,80,0,0"/>
        </Grid>
    </Grid>

</ContentPage>
